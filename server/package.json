{
  "name": "@amber-stash/server",
  "version": "0.1.0",
  "private": true,
  "description": "Amber Stash Cloudflare Worker — Anthropic API proxy with rate limiting, monthly quotas, and Stripe billing hooks.",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dry-run": "wrangler deploy --dry-run --outdir=dist",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.40",
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.6.0",
    "vitest": "2.1.9",
    "wrangler": "^3.78.0"
  }
}
